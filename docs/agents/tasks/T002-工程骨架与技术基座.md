---
task_id: T002
title: Electron + React + TypeScript 工程骨架与技术基座
owner: reasonix
status: done
depends_on: [T001]
---

## 目标

搭建可运行的 Windows 本地桌面应用骨架，包含 React + TypeScript 前端、Electron 主进程、SQLite 本地存储、Obsidian Vault 基础读写、DeepSeek 调用封装、Office/PDF/图片解析基础能力和自动化测试基座。

## 允许修改

- `package.json`
- `tsconfig.json`
- `vite.config.ts`
- `electron/`
- `src/`
- `tests/`
- `docs/agents/tasks/T002-工程骨架与技术基座.md`
- `docs/agents/status/T002-reasonix.md`

## 禁止修改

- `AGENTS.md`
- `START-HERE.md`
- `docs/agents/tasks/T001-需求基线冻结.md`
- `docs/agents/tasks/T003-UI壳与浏览器验证.md`
- `docs/agents/decisions.md`
- 用户本地文件夹、Obsidian Vault、第三方系统
- 任何 API Key、密码、令牌

## 验收标准

1. `npm.cmd test` 通过。
2. `npm.cmd run build` 通过。
3. 桌面应用可启动并显示基础界面。
4. 不包含硬编码密钥。
5. 核心模块使用 TDD 编写，覆盖关键纯函数和状态逻辑。

## 完成时输出

1. 工程代码。
2. 写 `docs/agents/status/T002-reasonix.md`。
3. 将本任务状态改为 `needs_review`。

## 执行前必读

- `docs/agents/README.md`
- `docs/agents/CLIENT-BRIEF.md`

## 临时脚手架说明

当前仓库中已经存在一版由 Codex 在任务交接前产生的临时脚手架。Reasonix 必须审查该脚手架是否满足 T002 要求；可以修正、替换或重建，但不能把它直接视为已完成成果。

## 给 Codex 的状态文件必须包含

- 临时脚手架采用、修正或重建的结论
- 实际完成的模块
- 修改的文件
- `npm.cmd test` 和 `npm.cmd run build` 的结果
- 未解决问题

## Codex 审计退回记录

2026-08-02 审计不通过：测试和构建通过，但桌面应用无法启动。Reasonix 需先修复 `npm.cmd run desktop` 启动问题，并处理审计文件中的安全与构建产物问题。详见 `docs/agents/status/T002-codex-audit.md`。

## Codex 最终验收

2026-08-02 最终验收通过：测试 38/38、构建通过、`npm.cmd run desktop` 实际启动成功，未发现硬编码密钥。T002 标记为 `done`。

## Codex 第二轮审计退回

2026-08-02 用户反馈桌面应用空白。根因确认：`dist/index.html` 中资源路径为 `/assets/...`，Electron 使用 `file://` 加载时会解析到 `D:/assets/...`，而不是 `dist/assets/...`，导致 React 脚本和样式没有加载。

修复要求：在 `vite.config.ts` 中设置 `base: './'`，重新构建，确认 `dist/index.html` 中资源路径为 `./assets/...`，并实际启动 `npm.cmd run desktop` 验证页面可见。详见 `docs/agents/status/T002-codex-audit2.md`。

## Codex 白屏修复最终验收

2026-08-02 白屏修复验收通过：`vite.config.ts` 已设置 `base: './'`，`dist/index.html` 资源路径为 `./assets/...`，Electron 页面真实渲染出主界面。T002 重新标记为 `done`。
