---
task_id: T003
title: 首版 UI 壳与浏览器验证
owner: zcode
status: done
depends_on: [T002]
---

## 目标

基于 T002 工程骨架实现首版桌面应用 UI：今日工作、统一收件箱、客户/项目、需求与任务、文档、知识库审核、设置七个模块，以及对话式操作、结构化卡片、目录选择、甘特图、需求池、对账导入和 HTML 原型预览入口。

## 允许修改

- `src/ui/`
- `src/App.tsx`
- `src/App.test.tsx`
- `src/styles/`
- `src/styles.css`
- `electron/preload.cjs`
- `docs/agents/verification/`
- `docs/agents/tasks/T003-UI壳与浏览器验证.md`
- `docs/agents/status/T003-zcode.md`

## 禁止修改

- `docs/agents/tasks/T001-需求基线冻结.md`
- `docs/agents/tasks/T002-工程骨架与技术基座.md`
- 后端核心存储、安全配置、API Key

## 验收标准

1. 七个主模块均可切换且不重叠。
2. 中文界面完整，按钮和文本不溢出。
3. 目录选择、需求输入、待确认卡片等关键交互可用。
4. 使用浏览器或桌面应用验证，保存 1280x720 和 390x844 截图。
5. 不影响 T002 构建和测试。

## 完成时输出

1. UI 代码。
2. 写 `docs/agents/status/T003-zcode.md`。
3. 将本任务状态改为 `needs_review`。

## 执行前必读

- `docs/agents/README.md`
- `docs/agents/CLIENT-BRIEF.md`

## 截图保存要求

在 `docs/agents/verification/` 保存：

- `T003-desktop-1280x720.png`
- `T003-mobile-390x844.png`

截图应显示实际页面内容，不能是空白页面或仅登录框。

## 给 Codex 的状态文件必须包含

- 实际实现的七个模块和核心交互
- 修改的文件
- 截图路径
- `npm.cmd test` 和 `npm.cmd run build` 的结果
- 未解决问题

## Codex 最终验收

2026-08-02 最终验收通过：测试 38/38、构建通过、Electron 桌面应用实际启动成功，桌面和移动端截图尺寸符合要求。T003 标记为 `done`。
