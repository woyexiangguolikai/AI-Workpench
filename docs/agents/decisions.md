# 决策记录

## 2026-08-02：GitHub 仓库关联确认

### 决策

- 确认 AI-Workpench 使用 GitHub 仓库：https://github.com/woyexiangguolikai/AI-Workpench
- 本地 Git 远端统一为 `origin`，默认分支为 `main`。
- 后续新对话可直接读取该仓库地址确认项目归属。

## 2026-08-02：三客户端协作模式

### 决策

- Codex 是中央大脑，负责需求、规划、架构、审查和总编排。
- ZCode 负责前端/UI、浏览器验证、文档类产出。
- Reasonix 负责独立实现、自动化测试、代码审查和修复。
- 所有任务状态以 `docs/agents/tasks/` 和 `docs/agents/status/` 为准。
- 同一时间只允许一个智能体处理同一个任务。
- 需求基线确认前不进入代码开发。

### 后续需要补充

- 用户提供 AI-Workpench 的产品目标和业务边界。
- 用户确认前端技术栈、后端技术栈和数据库。
- Codex 创建第一个正式任务 `T001`。

## 2026-08-02：重新访谈后的首版需求基线

### 决策

- 用户要求清除此前 AI-Workpench 需求记忆，并从零开始 grill-me。
- 首版定位：Windows 本地优先桌面应用，用户个人使用，数据模型预留多用户/角色/租户字段。
- 数据底座：现有本地文件夹 + WPS 文件；Obsidian Vault 作为知识底座，工作台直接读写 Markdown，双向实时同步。
- 首版主线：统一收件箱 -> 项目档案 -> 售前/招投标文档初稿 -> 自动生成待办和 DDL。
- 全量迁移 1.3GB 过往文件到 Vault，原始文件夹保留，支持去重、OCR/解析、分类和文档优化。
- 包含需求池、需求/任务排期、甘特图、测试用例、缺陷、HTML 高保真原型、月度对账、二开模块地图和源码只读分析。
- AI 主模型使用 DeepSeek 云端 API；API Key 不写入项目文件，设置页提示数据发送云端并保留调用记录。
- AI 自动处理重复整理，关键动作需用户确认；AI 从用户接受、修改、拒绝行为中学习并沉淀可审核的个人规则。
- 详细需求基线见 `docs/agents/tasks/T001-需求基线冻结.md`。
- 用户逐项验证需求基线达到预期后，T001 才可验收。

### 后续需要补充

- 用户提供标注 skill、随行付和系统后台 Excel 示例、源码目录示例。
- 用户逐项确认 T001 需求基线。
